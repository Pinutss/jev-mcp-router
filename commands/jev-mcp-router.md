---
name: jev-mcp-router
description: Select MCP tools under a context budget with mcp_select.
---

# JEV MCP Router

Use the `mcp_select` tool from the `jev-mcp` MCP server.

Required: query, tools.

Never pass API keys in the tool arguments.
